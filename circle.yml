machine:
  node:
    version: 6.7.0
  services:
    - docker

test:
  pre:
    - npm run build

deployment:
  production:
    branch: master
    commands:
      - npm run build:invoker
      - npm run deploy:invoker